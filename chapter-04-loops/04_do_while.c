#include<stdio.h>

int main(){
    
    int i = 220;

    do {
        printf("The value of i is %d\n", i);
    } while (i < 10);

    // While loop which executes atleast once
    // is do-while loop

    return(0);
}