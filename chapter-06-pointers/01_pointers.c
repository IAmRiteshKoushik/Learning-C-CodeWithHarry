#include<stdio.h>

int main(){

    // Declare a pointer
    int *j;
    // Other pointers  --
    // (Don't get confused with the naming scheme)
    // int *ch_ptr;
    // char *ch_ptr;
    // float *ch_ptr;

    return(0);
}